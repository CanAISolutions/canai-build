# Render Environment Variables Template
# These will be replaced by actual values from Render's environment

NODE_ENV=production
ENV=production
PORT=10000
CORS_ORIGIN=https://canai-solutions.com
VITE_API_BASE=https://canai-build.onrender.com